<h2>Gérer vos documents</h2>
<ul>
 <%= render :partial => "documents/documents", :object => @documents, :locals => { :linked => true, :empty => "Vous n'avez aucun document, vous devriez #{link_to("en ajouter un", :action => "add")}", :actions => true, :tags => true } %>
</ul>
<%= will_paginate @documents %>     
