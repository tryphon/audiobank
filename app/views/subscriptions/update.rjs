page.replace_html "users", :partial => "users/users", :object => User.find(:all, :conditions => ["id != ?", session[:user]]) - @document.subscribers, :locals => { :empty => "Déposer ici les utilisateurs qui n'ont accès à ce document" }
page.replace_html "subscribers", :partial => "subscribers/subscribers", :object => @document.subscribers, :locals => { :empty => "Déposer ici les utilisateurs qui ont accès à ce document" }
